#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define true 1
#define false 0

// #define INTERVAL (1)

int main(void) {
    printf("Simple timer\n");

// struct timeval initial ;

	// #getitimer(TIMER_REAL, );

	FILE *in = stdin;

	printf("file descriptor %d\n", in);
    return EXIT_SUCCESS;
}
